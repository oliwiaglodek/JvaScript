#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@eslint/create-config/bin/create-config.js" "$@"
  ret=$?
else 
  node  "$basedir/../@eslint/create-config/bin/create-config.js" "$@"
  ret=$?
fi
exit $ret
